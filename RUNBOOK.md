# RUNBOOK — 실전화(DRY_RUN=false) 가이드

> 순서대로 하면 됩니다. **각 단계의 확인 항목을 통과하기 전에 다음으로 넘어가지 마세요.**
> 원칙: 모든 것은 키가 없으면 휴면이고, 라이브에서 불확실하면 무조건 실패하게 되어 있습니다.

---

## 0. 사전 준비 (돈 안 드는 것부터)

- [ ] **KR 로컬 PC에서 실행** — 업비트/빗썸 API·공지는 KR IP 필요. `git pull && npm run build && npm start` (상시 운용은 dev 모드 말고 프로덕션 빌드)
- [ ] **텔레그램 봇**: @BotFather → 봇 생성 → `TELEGRAM_BOT_TOKEN`. 봇에게 아무 메시지 보낸 뒤 `https://api.telegram.org/bot<토큰>/getUpdates`에서 `chat.id` → `TELEGRAM_CHAT_ID`
  - 확인: 운영 탭 → 텔레그램 카드 "연결됨" → **테스트 발송** 클릭 → 폰 수신
- [ ] **상장따리 소스**: 로컬에서 `curl -s -H "User-Agent: Mozilla/5.0" "https://api-manager.upbit.com/api/v1/announcements?os=web&page=1&per_page=1&category=trade" | head -c 200`
  - JSON 나오면 공지 API 작동. HTML이면 `LISTING_TG_CHANNEL`에 신뢰하는 상장알림 텔레그램 채널명 설정
- [ ] **거래소 지갑 라벨 임포트** (상장따리 물량 신호용): 운영 탭 → "거래소 온체인 보유량" 카드 → **라벨 임포트** 1회 클릭 (Etherscan 공개 라벨 ~22MB → `data/exchange-wallets.json`). 이후 상장 공지 텔레그램에 거래소 핫/콜드 보유량이 자동 포함됨. 핫/콜드 재분류는 아캄 웹(intel.arkm.com) 참고해 파일에서 `type` 수정
- [ ] **상장 자동매수(선택)**: 상장 탭에서 켜기 + 규모 설정. 라이브 집행은 `LISTING_AUTO_LIVE=true` 추가 필요(이중 옵트인). TG 채널 여러 개는 `LISTING_TG_CHANNEL=chan1,chan2` (빗썸 키워드 자동 인식). 핫월렛 급증 알림 임계는 `LISTING_SURGE_USD_MIN`(기본 $25K/분)
- [ ] **개인지갑**: 새 지갑 생성(기존 주력 지갑 금지). `WALLET_PRIVATE_KEY`(EVM). 소액 ETH(가스)만 넣기
  - ⚠️ 이 키는 자금을 옮길 수 있음 — `.env.local`에만, 절대 커밋 금지 (`.gitignore`가 `.env*` 커버함)

## 1. 거래소 API 키 발급

각 거래소에서 **읽기+거래+출금** 권한 키 발급, 가능하면 **IP 화이트리스트에 로컬 PC IP** 등록.
키 입력은 **헤더 ⚙ 설정창**에서 (data/secrets.json에 0600으로 저장, .env.local보다 우선, 재시작 불필요):

| 거래소 | env | 참고 |
|---|---|---|
| 바이낸스 | `BINANCE_KEY/SECRET` | 현물+선물(USDT-M) 권한. 출금 권한은 실전 직전에 |
| 업비트 | `UPBIT_KEY/SECRET` | Open API — 자산조회·주문·출금 각각 체크 |
| 빗썸 | `BITHUMB_KEY/SECRET` | API 1.0. 수수료 쿠폰 있으면 `BITHUMB_TAKER_PCT=0.04` |
| 바이비트 | `BYBIT_KEY/SECRET` | 선택(글로벌 다리 확장용) |
| OKX 거래 | `OKX_KEY/SECRET/PASSPHRASE` | 선택 |
| OKX Web3 | `OKX_WEB3_KEY/SECRET/PASSPHRASE` | cex-dex·상장 DEX 매수용, 거래 키와 별개 |
| OKX Web3 프로젝트 | `OKX_WEB3_PROJECT` | 선택 — 개발자포털 Project ID. 있으면 전송 가스·논스를 OKX가 빌드(없으면 ethers 자체 추정) |

**키 넣고 재시작 후 확인 (아직 DRY_RUN=true):**
- [ ] 자산 탭에 실잔고 표시 (데모 배지 사라짐)
- [ ] 입출금 게이트 조회에 업비트/바이낸스 컬럼 등장 ("키필요" 사라짐)
- [ ] 보드의 executable(실행 버튼 활성)이 **줄어듦** — 라이브 fail-closed 게이트가 "확인된 열림"만 통과시키기 시작 (정상)

## 2. 사전 등록 (거래소 웹에서, 최초 1회)

- [ ] **국내→해외 출금 화이트리스트**: 업비트/빗썸에 바이낸스(등) 입금주소 등록 (역프·크로스용)
- [ ] **트래블룰**: 업비트에 개인지갑 주소 등록 (정프 개인지갑 경유 입금용)
- [ ] KRW 입금 후 **72시간 출금 정지** 유의 — 실테스트 자금은 미리 넣어두기

## 3. 안전장치 세팅 (`.env.local`)

```bash
DRY_RUN=true            # 아직 유지!
EXEC_TOKEN=<긴 랜덤 문자열>   # openssl rand -hex 24
RISK_MAX_PER_TRADE_USD=100    # 실테스트 동안 소액으로 잠금
RISK_MAX_INFLIGHT_USD=200
RISK_MAX_DAILY_LOSS_USD=30
```
- [ ] 운영 탭에서 한도 표시 확인, 킬스위치 눌렀다 해제 동작 확인
- [ ] 서버는 **localhost에만 바인딩**돼 있는지 확인 (외부 노출 금지). 폰에서 보려면 같은 공유기 내에서만

## 4. DRY_RUN 최종 리허설 (키 있는 상태로)

- [ ] 김프 기회 하나 열어 **실행 시작** → 전 단계 시뮬 통과, 재견적·타임라인·tx칩 확인
- [ ] 모달 닫고 **브라우저 탭까지 닫아도** 운영 탭에서 런 지속 확인 (실행 루프는 서버가 소유)
- [ ] cex-dex 기회 하나 열어 스왑 미리보기(예상 수령·슬리피지·유동성 등급) 확인
- [ ] 스마트 청산 25% 눌러 시뮬 확인

## 5. 첫 실거래 (소액 · 단계 수동)

**추천 시나리오: XRP 정프 $50** (전송 1분·태그 자동처리·유동성 최상)

1. `DRY_RUN=false` 로 변경, 재시작
2. 보드에서 XRP(또는 net>0 확인된 코인) 실행 모달 열기
3. 수량 **$50**, 헷지 ON, 자동범위 **수동**
4. 단계마다 확인하며 진행:
   - [ ] 매수: 바이낸스 앱에서 실체결 교차 확인
   - [ ] 헷지: 선물 포지션 -50 XRP 확인
   - [ ] 출금 승인 전 **주소·태그 육안 확인** (XRP는 태그 필수 — 태그 없으면 자동 차단됨)
   - [ ] 입금 확인: 업비트 입금 내역과 대조
   - [ ] 매도·헷지 청산·정산: 운영 탭 거래·손익 카드에 기록 확인
5. **실현 net vs 보드 표시 net 차이(누수)** 기록 — 이게 비용 모델 보정 데이터

문제 발생 시: **킬스위치**(전체 중단) → 수동으로 거래소에서 정리 → 원인 파악 후 재개.

## 6. 점진 확대

- [ ] 소액 3~5회 성공 후 `RISK_MAX_PER_TRADE_USD` 단계적 상향
- [ ] 자동범위 수동 → **출금 전까지** → 익숙해지면 매도 전/전자동
- [ ] 운영 탭 "탐지 vs 실현 평균 누수"가 안정되면 그 값만큼 비용 모델(env) 보정

## 트러블슈팅

| 증상 | 원인/조치 |
|---|---|
| 실행 버튼 전부 비활성 | 라이브 fail-closed — 게이트 미확인. 키 권한(자산조회) 확인 |
| "EXEC_TOKEN 설정 필수" | env 누락 또는 재시작 안 함 |
| 출금 단계 "태그 미확인 차단" | 정상 방어 — 목적지 태그 조회 실패. 키 권한/코인 지원 확인 |
| "선물 가용 마진 부족" | 선물 지갑에 USDT 이체 (헷지 노티널의 60%+) |
| 텔레그램 하트비트 안 옴 | 서버 죽음 — 재시작. (하트비트가 곧 생존 신호) |
| 스캔 정지 경고 | 재시작. 반복되면 `data/` 로그 확인 후 이슈 공유 |
| 매수 단계 "최소 출금 수량 미달" | 부분체결로 수량이 거래소 최소 출금 미만 — 규모를 키우거나 중단 (정상 방어) |
| "기회 스냅샷 2분 초과" | 보드가 오래됨 — 갱신 후 다시 실행 (낡은 판단으로 진입 차단) |
| "롤백 보류: 슬리피지 초과" | 롤백 시장가가 얇은 호가에 물릴 판 — 자동 보류 + 텔레그램. 수동 정리 |
| 재시작 후 "실행 루프 중단" 경고 런 | 서버 재시작으로 끊긴 런 — 자동 재개 안 함. 거래소 실포지션·헷지 수동 확인 후 정리 |
| 상장 상세/핫월렛 빈 화면 | CoinGecko 레이트리밋 — 재시도(실패는 더는 오래 캐시 안 됨). 잦으면 조회 자제 |
| 화면이 가끔 몇 초씩 느림 | **먼저 코드 말고 메모리를 본다** — 아래 "느림 진단" 참조 |

### 느림 진단 — API가 느린 건가, 프로세스가 멈춘 건가

이 둘은 화면에서 똑같이 보이지만 고치는 곳이 다르다. 순서대로 본다.

1. **화면 숫자** — 상세 패널이 2초 넘게 걸리면 "첫 응답 4.5s"가 패널에 남는다.
2. **서버 로그** (`pm2 logs` 또는 `next start` 로그)
   ```
   [listing-detail] EUL fast 2947ms — token 1355ms · cex 80ms … | 대기후 1592ms ⚠멈춤
   [loop] 멈춤 501ms (최근 5초) · rss 114MB
   ```
   - `token`/`cex`가 크다 → 외부 API가 늦은 것.
   - **`대기후`가 크다 / `[loop] 멈춤`이 찍힌다 → 프로세스가 멈춘 것.** 이 구간엔
     계산밖에 없으므로 네트워크로는 설명되지 않는다.
3. **멈춤이면 메모리를 본다**
   ```bash
   free -m                       # Swap used 가 수백 MB면 이미 스와핑 중
   vmstat 5 3                    # si/so 가 0이 아니면 지금 스왑 트래픽이 흐르는 중
   ps -eo pid,pmem,rss,comm --sort=-rss | head
   ```
   이 박스는 RAM 955MB다. `next build`(400~600MB)나 다른 큰 프로세스가 함께 뜨면
   커널이 next-server 힙을 스왑으로 밀어내고, 그다음 GC나 힙 접근이 **초 단위로**
   멈춘다. 코드는 멀쩡한데 응답만 늦는 전형적인 모습이다.
   - 즉효: 사용 중엔 빌드를 돌리지 않는다. 안 쓰는 pm2 앱을 내린다.
   - 완화: `sysctl -w vm.swappiness=10` (앱 힙보다 페이지 캐시를 먼저 버리게 한다)
   - 근본: RAM을 늘리거나 KR 박스로 이전 (TODO.md P1)

## 상시 운용

```bash
npm run build && npm start   # 프로덕션 모드 (dev보다 안정·빠름)
```
- 재시작해도 히스토리·일일손실·상장 플레이·**진행 중이던 런**은 `data/state/`(섹션별 파일)에서 복원됨 (중단된 런은 자동 재개 없이 경고로만 — 수동 확인). 예전 단일 `data/state.json`은 처음 읽을 때 자동 마이그레이션되고 원본은 보존됨
- 브라우저는 꺼도 됨 — 감시·알림은 서버가 함(부팅 시 자동 무장). 폰은 텔레그램으로

### pm2 자동 재시작 (권장)

```bash
npm i -g pm2
npm run build
pm2 start ecosystem.config.js            # 프로덕션 빌드로 기동 + 크래시 시 자동 재시작
pm2 save && pm2 startup                  # 부팅 시 자동 시작 (안내 명령 실행)
pm2 logs arb                             # 로그 확인
```
> `next start`(프로덕션)로 돌린다. dev 모드 상시 운용은 webpack·소스맵이 상주해 메모리를 계속 먹고, 실행 중에도 파일 변경에 재컴파일한다.

### ⚠ 재빌드하면 **반드시 재시작** — 안 하면 화면이 검게 뜬다

```bash
npm run build && pm2 restart arb     # 빌드만 하고 끝내지 말 것
```

`next build`는 청크 파일명에 새 해시를 붙인다. 실행 중인 프로세스는 **기동 시점의
빌드**를 기준으로 HTML을 만들므로, 재빌드 후 재시작하지 않으면 HTML이 더 이상
존재하지 않는 청크를 가리킨다 → 그 요청이 400/404 → **클라이언트 JS가 로드되지
않아 검은 화면만 뜬다**(SSR HTML은 멀쩡해서 서버 로그엔 아무 에러도 안 남는다).

증상이 나오면 이렇게 확인한다:

```bash
curl -s localhost:3100/ | grep -oE 'app/page-[a-f0-9]+\.js'   # HTML이 요구하는 청크
ls .next/static/chunks/app/ | grep '^page-'                    # 디스크에 있는 청크
```

두 해시가 다르면 재시작하면 끝. 재시작 후에도 폰에 남아 있으면 하드 새로고침
(브라우저가 옛 HTML을 캐싱했을 수 있다).

프로세스를 직접 죽일 땐 **PID로** 죽인다 — `next start`/`next dev`의 실제 리스너는
자식 프로세스 `next-server`라서 `pkill -f "next start"` 패턴에 걸리지 않는다.

```bash
ss -tlnp | grep 3100      # PID 확인 후 kill -9 <pid>
```

### 헬스체크 crontab — **필수** (무장 + 복구를 둘 다 한다)

```bash
crontab -e
* * * * * curl -sf localhost:3100/api/health >/dev/null
```

```bash
curl -s localhost:3100/api/health
# {"ok":true,"scanAgeSec":2,"liveOpps":30,"killed":false,"dryRun":true,"uptimeSec":123,"rssMb":180}
# 스캔 60초 이상 멈추면 503 — uptime 모니터(UptimeRobot 등)에도 등록 가능
```

**왜 필수인가 — 두 가지를 동시에 해결한다:**

1. **무장**: 스캔 루프·상장 공지 감시·워치독은 첫 HTTP 요청에서 켜진다. 무인 재시작 후 아무도
   브라우저를 열지 않으면 **모트인 공지 감시가 꺼진 채로 있고, 그걸 알려줄 워치독도 함께 꺼져
   있다.** 1분 크론이 이걸 확실히 켜준다.
2. **복구**: 이 앱의 최악 고장은 "프로세스는 살아 있는데 스캔만 멈추는 것"이다. 그러면 WS가 보드를
   살아 보이게 하는 동안 라이브 진입은 신선도 게이트에 전부 막힌다. 프로세스가 죽지 않으니
   **pm2는 이 고장을 잡지 못한다.** `/api/health`는 낡은 스냅샷·걸린 갱신 래치를 보면 새 스캔을
   띄우므로 그 구멍이 메워진다. (서버 내부 워치독도 3분마다 자체 복구를 시도한다 — 텔레그램
   설정 여부와 무관하게.)
- 크래시 시 텔레그램으로 💥 통보가 감 (pm2가 살려도 "죽었었다"는 건 알림)
- 스캔 정지·기회 소멸·24h 하트비트는 기존 워치독이 텔레그램으로 알림
